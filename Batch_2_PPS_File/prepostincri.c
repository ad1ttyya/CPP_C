#include<stdio.h>
void main()
{
    // int a=200;
    // b=++a + ++a + ++a;
    // printf("%d\n",a);
    int a;
    scanf("%d",&a);
    switch (a)
    {
    case 0:
        printf("a is zero");
        break;
    
    default:
        break;
    }
}